package context


object SumInject<Long> extends InjectObject<Long>

    func init  { sum = Long    } 

    override func eval: (Long item)   { sum = sum + item    } 

    override func result -> Long  = sum;

    private var Long sum

end

